{
  "name": "tw-html",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
	"build": "postcss ./resource/css/main.css -o resource/css/styles.css",
	"dev": "npm run build && live-server --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.4",
    "postcss-cli": "^9.1.0",
    "tailwindcss": "^3.0.23"
  }
}
